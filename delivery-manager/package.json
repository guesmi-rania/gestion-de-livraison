{
  "name": "delivery-manager",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://guesmi-rania.github.io/gestion-de-livraison/",  // Remplace ton-utilisateur par ton GitHub username
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",  // Prépare la build avant de déployer
    "deploy": "npm run predeploy && gh-pages -d dist"  // Déploie après la build
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.0",
    "gh-pages": "^6.3.0",
    "typescript": "~5.7.2",
    "vite": "^4.4.0",
    "vite-plugin-gh-pages": "^1.0.1"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "react": "^18.2.0",  
    "react-dom": "^18.2.0"
}
